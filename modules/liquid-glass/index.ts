// Local Expo module: Android native liquid-glass bridge.
//
// The native Android view is consumed directly via
// `requireNativeView('LiquidGlass')` in
// app/src/components/GlassBackground.android.tsx, so there is no extra JS
// surface to export here. This module exists for its native (Kotlin) side and
// is picked up by Expo autolinking from the `modules/` directory.
export {};
